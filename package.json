{
  "name": "veklang",
  "main": "./src/index.ts",
  "scripts": {
    "build": "rm -rf build && tsc",
    "clean": "rm -rf node_modules build",
    "format": "biome format --write .",
    "lint": "biome lint --write ."
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
